---
// SimpleBanner component that shows a warning for Spanish translations being in Beta
const currentLocale = Astro.currentLocale;

// Only show the banner for Spanish locale
const shouldShowBanner = currentLocale === "es";
---

{
  shouldShowBanner && (
    <div class="beta-translation-banner">
      <div class="banner-content">
        <span class="banner-icon">⚠️</span>
        <span class="banner-text">
          Las traducciones al español están en versión Beta. Si encuentras errores o tienes
          sugerencias,
          <a
            href="https://github.com/aptos-labs/aptos-docs/issues/new"
            target="_blank"
            rel="noopener noreferrer"
          >
            ayúdanos a mejorarlas.
          </a>
        </span>
      </div>
    </div>
  )
}

<style>
  .beta-translation-banner {
    background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
    border: 1px solid #f59e0b;
    border-radius: 0.5rem;
    padding: 0.75rem 1rem;
    margin: 1rem 0;
    text-align: center;
    font-size: 0.875rem;
    line-height: 1.25rem;
    color: #92400e;
    max-width: 100%;
  }

  .banner-content {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .banner-icon {
    font-size: 1rem;
    flex-shrink: 0;
  }

  .banner-text {
    flex: 1;
  }

  .banner-text a {
    color: #d97706;
    text-decoration: underline;
    font-weight: 500;
  }

  .banner-text a:hover {
    color: #b45309;
  }

  /* Dark mode styles */
  @media (prefers-color-scheme: dark) {
    .beta-translation-banner {
      background: linear-gradient(135deg, #451a03 0%, #78350f 100%);
      border: 1px solid #f59e0b;
      color: #fef3c7;
    }

    .banner-text a {
      color: #fbbf24;
    }

    .banner-text a:hover {
      color: #fde68a;
    }
  }

  /* Responsive design */
  @media (max-width: 768px) {
    .beta-translation-banner {
      padding: 0.5rem 0.75rem;
      font-size: 0.8rem;
      margin: 0.75rem 0;
    }

    .banner-content {
      flex-direction: column;
      gap: 0.25rem;
    }
  }
</style>

<script>
  // Add a class to the body when banner is present
  if (document.querySelector(".beta-translation-banner")) {
    document.body.classList.add("has-beta-banner");
  }
</script>

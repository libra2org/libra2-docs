---
title: "Unity Integration"
---

import { Aside, CardGrid, LinkCard, Steps } from '@astrojs/starlight/components';

<Aside type="caution" emoji="❗">
  This integration is currently in beta. Please report any issues you encounter
  by creating an issue in the
  [libra2-core/aptos-dotnet-sdk](https://github.com/libra2org/aptos-dotnet-sdk)
  repository.
</Aside>

This guide will walk you through the process of integrating the Libra .NET SDK.

<Steps>
  1. Install the Libra Unity SDK

     #### Option 1: Import via Unity Package Manager (UPM)

     1. Open Package Manager window (Window | Package Manager)
     2. Click + button on the upper-left of a window, and select _Add package from git URL..._
     3. Enter the following URL and click Add button

     ```shellscript
     https://github.com/libra2org/unity-sdk.git?path=/Packages/com.aptoslabs.aptos-unity-sdk
     ```

     #### Option 2: Import via `unitypackage`

     1. Go to the [`libra2-core/unity-sdk Releases`](https://github.com/libra2org/unity-sdk/releases) and download the latest release.
     2. Drag and drop the `.unitypackage` file into your Unity project.

  2. Use the Libra SDK

     Import the `Libra` namespace in your C# script and use the SDK.

     ```csharp {2-2,8-8,11-15}
     using UnityEngine;
     using Libra;

     class Example : MonoBehaviour
     {
         public void Start()
         {
             PrintLedgerInfo();
         }

         async void PrintLedgerInfo() {
             var client = new LibraUnityClient(Networks.Mainnet);
             var ledgerInfo = await client.Block.GetLedgerInfo();
             Debug.Log(ledgerInfo.BlockHeight);
         }

     }
     ```
</Steps>

## Next Steps

You've successfully integrated the Libra .NET SDK into your Unity project. Now
you can start building your game and interacting with the Libra blockchain. Below
are some resources to help you get started.

<CardGrid>
  <LinkCard href="/build/sdks/dotnet-sdk/getting-started" title="Getting Started" description="Begin developing using the Libra .NET SDK." />

  <LinkCard href="/build/sdks/unity-sdk" title="Unity SDK" description="Overview of the Unity SDK." />

  <LinkCard href="https://github.com/libra2org/aptos-unity-starter" title="Libra Wallet Starter" description="Example Unity project with an integration of the Libra Unity SDK." target="_blank" />
</CardGrid>
